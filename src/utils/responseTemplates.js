const { SOURCE_CODE_URL } = require('../config');

const templates = {
  sourceCode: `🤖 Here's my source code: ${SOURCE_CODE_URL}\n\nFeel free to check it out! ⭐️`
};

module.exports = templates;